services:

  postgres:
    image: postgres:17.10

    container_name: pg_hw4

    environment:
      POSTGRES_DB: homework_4
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: 1234

    ports:
      - "5434:5432"

    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: